<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products - Grocery Management</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 30px 20px;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
            animation: fadeIn 0.6s ease-out;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .header {
            background: white;
            border-radius: 20px;
            padding: 30px 40px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 20px;
        }
        
        .header h1 {
            font-size: 2.5em;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .nav-buttons {
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
        }
        
        .btn {
            padding: 12px 30px;
            border-radius: 25px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            border: none;
            cursor: pointer;
            font-size: 1em;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.6);
        }
        
        .btn-secondary {
            background: white;
            color: #667eea;
            border: 2px solid #667eea;
        }
        
        .btn-secondary:hover {
            background: #667eea;
            color: white;
            transform: translateY(-2px);
        }
        
        .table-container {
            background: white;
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            overflow-x: auto;
        }
        
        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
        }
        
        thead {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        
        thead th {
            padding: 20px;
            color: white;
            font-weight: 600;
            text-align: left;
            font-size: 1.1em;
            border: none;
        }
        
        thead th:first-child {
            border-top-left-radius: 15px;
        }
        
        thead th:last-child {
            border-top-right-radius: 15px;
        }
        
        tbody tr {
            transition: all 0.3s ease;
            border-bottom: 1px solid #f0f0f0;
        }
        
        tbody tr:hover {
            background: linear-gradient(90deg, #f8f9ff 0%, #fff5f8 100%);
            transform: scale(1.01);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }
        
        tbody td {
            padding: 20px;
            color: #333;
            font-size: 1em;
        }
        
        .actions {
            display: flex;
            gap: 10px;
            align-items: center;
            flex-wrap: wrap;
        }
        
        .action-link {
            padding: 8px 18px;
            border-radius: 20px;
            text-decoration: none;
            font-size: 0.9em;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-block;
        }
        
        .view-link {
            background: #4facfe;
            color: white;
        }
        
        .view-link:hover {
            background: #00c6ff;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(79, 172, 254, 0.4);
        }
        
        .edit-link {
            background: #43e97b;
            color: white;
        }
        
        .edit-link:hover {
            background: #38f9d7;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(67, 233, 123, 0.4);
        }
        
        .delete-form {
            display: inline;
        }
        
        .delete-btn {
            padding: 8px 18px;
            border-radius: 20px;
            background: #f093fb;
            color: white;
            border: none;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 0.9em;
        }
        
        .delete-btn:hover {
            background: #f5576c;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(245, 87, 108, 0.4);
        }
        
        .price {
            font-weight: 700;
            color: #667eea;
            font-size: 1.1em;
        }
        
        .quantity-badge {
            display: inline-block;
            padding: 6px 15px;
            border-radius: 15px;
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            color: white;
            font-weight: 600;
        }
        
        .expiry {
            color: #666;
            font-style: italic;
        }
        
        .no-products {
            text-align: center;
            padding: 60px 20px;
            color: #999;
            font-size: 1.2em;
        }
        
        @media (max-width: 768px) {
            .header {
                flex-direction: column;
                align-items: flex-start;
            }
            
            .header h1 {
                font-size: 2em;
            }
            
            .table-container {
                padding: 15px;
            }
            
            table {
                font-size: 0.9em;
            }
            
            thead th, tbody td {
                padding: 12px 8px;
            }
            
            .actions {
                flex-direction: column;
                align-items: flex-start;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📦 All Products</h1>
            <div class="nav-buttons">
                <a href="/products/new" class="btn btn-primary">➕ Add New Product</a>
                <a href="/" class="btn btn-secondary">🏠 Home</a>
            </div>
        </div>
        
        <div class="table-container">
            <% if (products.length === 0) { %>
                <div class="no-products">
                    No products found. Add your first product to get started!
                </div>
            <% } else { %>
                <table>
                    <thead>
                        <tr>
                            <th>Product Name</th>
                            <th>Firm</th>
                            <th>Price</th>
                            <th>Quantity</th>
                            <th>Expiry Date</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% products.forEach(p => { %>
                            <tr>
                                <td><strong><%= p.Product_Name %></strong></td>
                                <td><%= p.Firm_Name %></td>
                                <td><span class="price">$<%= p.Price %></span></td>
                                <td><span class="quantity-badge"><%= p.Quantity %></span></td>
                                <td><span class="expiry"><%= new Date(p.Expiry_Date).toLocaleDateString() %></span></td>
                                <td>
                                    <div class="actions">
                                        <a href="/products/<%= p._id %>" class="action-link view-link">👁️ View</a>
                                        <a href="/products/<%= p._id %>/edit" class="action-link edit-link">✏️ Edit</a>
                                        <form action="/products/<%= p._id %>/delete" method="post" class="delete-form">
                                            <button type="submit" class="delete-btn" onclick="return confirm('Are you sure you want to delete this product?')">🗑️ Delete</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } %>
        </div>
    </div>
</body>
</html>
